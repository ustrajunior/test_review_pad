api-version: reviewpad.com/v3.x

rules:
  - name: changed-router
    description: Patch contains changes to the router
    spec: '$hasFilePattern("**/router.ex")'

workflows:
  - name: comment-on-router-changes
    if:
      - rule: changed-router
    then:
      - $warn("testing comment")
